export function canvasToBlob(canvas){return new Promise((resolve,reject)=>canvas.toBlob(blob=>blob?resolve(blob):reject(new Error('PNGを作成できませんでした。')),'image/png'));}
export async function copyCanvasImage(canvas){if(!window.ClipboardItem||!navigator.clipboard?.write)throw new Error('このブラウザは画像コピーに未対応です。Chrome / Edge / Safariの最新版をお試しください。');const blob=await canvasToBlob(canvas);await navigator.clipboard.write([new ClipboardItem({'image/png':blob})]);return blob;}
export async function downloadCanvasPng(canvas,filename){const blob=await canvasToBlob(canvas),url=URL.createObjectURL(blob),link=document.createElement('a');link.href=url;link.download=filename;link.click();setTimeout(()=>URL.revokeObjectURL(url),1000);return blob;}
export async function copyText(text){if(navigator.clipboard?.writeText)return navigator.clipboard.writeText(text);const area=document.createElement('textarea');area.value=text;area.style.position='fixed';area.style.opacity='0';document.body.append(area);area.select();document.execCommand('copy');area.remove();}
