export function isSupportedImage(file){return Boolean(file&&['image/png','image/jpeg','image/webp'].includes(file.type));}
export function imageFileFromTransfer(transfer){if(!transfer)return null;const item=[...(transfer.items||[])].find(value=>value.kind==='file'&&value.type.startsWith('image/'));return item?.getAsFile()||[...(transfer.files||[])].find(file=>file.type.startsWith('image/'))||null;}
export function loadImageFile(file){if(!isSupportedImage(file))return Promise.reject(new Error('PNG / JPEG / WebP画像を選んでください。'));return new Promise((resolve,reject)=>{const url=URL.createObjectURL(file),image=new Image();image.onload=()=>{URL.revokeObjectURL(url);resolve(image);};image.onerror=()=>{URL.revokeObjectURL(url);reject(new Error('画像を読み込めませんでした。'));};image.src=url;});}
